<!DOCTYPE html>
<html lang="pt-br">
<head>
    <%- include("partials/header") %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Excluir Tarefa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-center">
            <form class="w-50 p-4 bg-light rounded shadow-sm text-center needs-validation" novalidate 
                  action="/tasks/<%= id %>/delete" method="POST">
                
                <h2 class="text-danger mb-4">Excluir Tarefa</h2>
                
                <!-- Mensagem de confirmação -->
                <p class="mb-4">Tem certeza que deseja excluir a tarefa "<strong><%= taskName %></strong>"?</p>


                <div class="d-flex justify-content-between mt-4">
                    <!-- Link para voltar -->
                    <a href="/tasks" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-2"></i>Voltar
                    </a>
                    
                    <!-- Botão para confirmar exclusão -->
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Sim, Excluir
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
<%- include("partials/footer") %>
</html>
