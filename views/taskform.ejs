<!DOCTYPE html>
<html lang="pt-br">
    <%- include('partials/header') %>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro de Tarefas</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2 class="text-center mb-4"><%= task ? "Editar" : "Cadastrar" %> Tarefa</h2>
    <form action="<%= task ? `/tasks/update/${task.id}` : '/tasks/new' %>" method="POST">
      <div class="mb-3">
        <label for="name" class="form-label">Nome da Tarefa:</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= task ? task.name : '' %>" required>
      </div>
      <div class="mb-3">
        <label for="description" class="form-label">Descrição da Tarefa:</label>
        <textarea class="form-control" id="description" name="description" rows="3" required><%= task ? task.description : '' %></textarea>
      </div>
      <div class="form-check mb-3">
        <input class="form-check-input" type="checkbox" id="completed" name="completed" <%= task && task.completed ? 'checked' : '' %>>
        <label class="form-check-label" for="completed">
          Tarefa finalizada
        </label>
      </div>
      <div class="d-grid">
        <button type="submit" class="btn btn-primary"><%= task ? "Atualizar" : "Cadastrar" %></button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
<%- include('partials/footer') %>
</html>
